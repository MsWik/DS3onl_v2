# Выполняем импорт базового образа python (с DockerHub)
# В качестве имени образа, указываем 'python', а в качестве версии - 'latest'

FROM python:latest

# Импортируем файлы 'server.py' и 'index.html' для запуска в контейнере кода, написанного на Python
# Параметр 'server.py' - имя файла, хранящегося на компьютере
# Параметр '/server/' - это путь, по которому нужно раместить указанный файл в образе

ADD server.py /server/
ADD index.html /server/

# Изменяем рабочую директорию образа
# В качестве директории, в которой будут выполняться команды, мы устанавливаем '/server/'

WORKDIR /server/